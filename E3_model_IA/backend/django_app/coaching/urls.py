from django.urls import path
from . import views

app_name = 'coaching'

urlpatterns = [
    # TODO: Ajouter les vues de coaching
    # path('', views.CoachingView.as_view(), name='coaching'),
    # path('plans/', views.TrainingPlanListView.as_view(), name='training_plans'),
]