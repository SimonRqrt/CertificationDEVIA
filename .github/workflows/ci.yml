name: CI Simple

on:
  push:
    branches: [ developp ]
  pull_request:
    branches: [ developp ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test Docker builds
        run: |
          echo "🔨 Test des builds Docker..."
          
          # Test Django
          docker build -f deployment/django.Dockerfile -t test-django .
          echo "✅ Django OK"
          
          # Test FastAPI
          docker build -f deployment/fastapi.Dockerfile -t test-fastapi .
          echo "✅ FastAPI OK"
          
          # Test Streamlit
          docker build -f deployment/streamlit.Dockerfile -t test-streamlit .
          echo "✅ Streamlit OK"
          
          echo "🎉 Tous les builds sont OK !"
